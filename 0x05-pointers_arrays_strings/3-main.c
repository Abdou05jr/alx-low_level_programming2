#include "main.h"

/**
 * main- check the code 
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "Ido not fear computers. I fear thr lack of them - Isaac Asimov";
	_puts(str);
        return (0);
}
